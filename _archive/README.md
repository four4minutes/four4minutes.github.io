# Archived template material

This directory stores AcademicPages sample pages, sample collection items, sample assets, and helper tools that are not part of the public website.

Jekyll does not build this directory because it is not configured as a collection. Move files back to their original locations only if they become part of the site again.
